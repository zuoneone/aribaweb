plugins {
    id 'war'
}

description = 'AribaWeb GuestBook Example'

dependencies {
    implementation project(':widgets')
    implementation project(':metaui')
    
    compileOnly files("${rootProject.projectDir}/lib/ext-build/javax.servlet.zip")
}

sourceSets {
    main {
        java {
            srcDirs = ['.']
            exclude 'build/**'
        }
        resources {
            srcDirs = ['.']
            include '**/*.awl', '**/*.properties', '**/*.xml', '**/*.png', '**/*.jpg', '**/*.gif', '**/*.js', '**/*.css'
            exclude 'build/**'
        }
    }
}

war {
    archiveFileName = 'GuestBook.war'
}
